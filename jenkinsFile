node('master') {
   def mvnHome

   stage('Setup') {
      mvnHome = tool 'M3'
   }

   stage('Build') {
         sh "'${mvnHome}/bin/mvn' -Dmaven.test.failure.ignore clean verify -DSERVICE_NAME=tiger"
   }

}